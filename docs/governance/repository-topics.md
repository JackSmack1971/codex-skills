# Repository topics

The repository topics are intentionally limited to evidence in the tree:

- `codex` — Codex skill packages and control-plane compatibility surfaces.
- `ai-agents` — agent skills and evaluation workflows.
- `github-actions` — the repository's validation workflow and automation.
- `python` — the validation and evaluation scripts.

Avoid adding marketing, framework, or ecosystem topics without corresponding
repository evidence.
