# Integration guide: security-best-practices

## Principle

Instrument high-value semantic boundaries only. Do not turn the target `SKILL.md` into an event-by-event logging checklist.

## Minimum semantic surface

1. Establish a run when the target skill is actually selected or when a target-owned entry script begins.
2. Emit `decision` only for branches that materially change workflow, safety, or verification.
3. Emit `verification`, `failure`, and `retry` around evidence-bearing checks.
4. Emit `user.correction` only when a correction is explicitly observed; never infer one.
5. Close the run with `run.finished` and an outcome.

The recorder prints the `run_id` on `start`. Pass that ID explicitly to later semantic events. Full commands may be supplied to `--command`; the recorder hashes them instead of storing them under the default policy.

## Static candidates from the target

These are inspection hints, not runtime facts.

### Decision candidates

- `LICENSE.txt:20` — otherwise, or (ii) ownership of fifty percent (50%) or more of the
- `LICENSE.txt:59` — excluding communication that is conspicuously marked or otherwise
- `LICENSE.txt:77` — use, offer to sell, sell, import, and otherwise transfer the Work,
- `LICENSE.txt:81` — with the Work to which such Contribution(s) was submitted. If You
- `LICENSE.txt:106` — (d) If the Work includes a "NOTICE" text file as part of its
- `LICENSE.txt:113` — documentation, if provided along with the Derivative Works; or,
- `LICENSE.txt:114` — within a display generated by the Derivative Works, if and
- `LICENSE.txt:127` — reproduction, and distribution of the Work otherwise complies with
- `LICENSE.txt:130` — 5. Submission of Contributions. Unless You explicitly state otherwise,
- `LICENSE.txt:143` — 7. Disclaimer of Warranty. Unless required by applicable law or
- `LICENSE.txt:154` — whether in tort (including negligence), contract, or otherwise,
- `LICENSE.txt:155` — unless required by applicable law (such as deliberate and grossly
- `LICENSE.txt:162` — other commercial damages or losses), even if such Contributor
- `LICENSE.txt:171` — any other Contributor, and only if You agree to indemnify,
- `LICENSE.txt:197` — Unless required by applicable law or agreed to in writing, software

### Verification candidates

- `SKILL.md:13` — - **Output:** Return the skill's named artifact or decision, with evidence, unresolved assumptions, and validation results.
- `SKILL.md:74` — - Consider authentication, authorization, tenant ownership, validation,
- `references/golang-general-backend-security.md:16` — - MUST treat uncertainty honestly: if a control might exist in infrastructure (reverse proxy, WAF, service mesh, platform config), report it as “not visible in app code; verify at runtime/config.”
- `references/golang-general-backend-security.md:45` — 5) AuthN/AuthZ boundaries, session/cookie settings, token validation.
- `references/golang-general-backend-security.md:81` — - False positive notes: what to verify if uncertain (edge configs, proxy behavior, auth assumptions)
- `references/golang-general-backend-security.md:101` — - SHOULD set a consistent set of security headers (or verify they are set at the edge).
- `references/golang-general-backend-security.md:136` — - Add an automated check (CI) that fails when Go is below an approved minimum.
- `references/golang-general-backend-security.md:178` — - MUST NOT hard-code secrets in Go source, test fixtures that may reach production, or build args.
- `references/golang-general-backend-security.md:214` — - Check for reverse proxies; even with a proxy, app-level timeouts still matter.
- `references/golang-general-backend-security.md:242` — - Look for “upload” endpoints and check limits.
- `references/golang-general-backend-security.md:246` — - For multipart, set conservative limits and validate file sizes/part counts explicitly.
- `references/golang-general-backend-security.md:272` — - Check whether `http.DefaultServeMux` is used and whether any debug handlers register globally.
- `references/golang-general-backend-security.md:298` — - Inspect ingress/proxy configs; if not visible, mark as “verify at edge”.
- `references/golang-general-backend-security.md:320` — - These headers may be set via reverse proxy/CDN; if not visible in app code, report as “verify at edge”.
- `references/golang-general-backend-security.md:401` — - Reflecting `Origin` without validation.

### Execution candidates

- `references/golang-general-backend-security.md:471` — - `os.Open(filepath.Join(baseDir, userPath))` without checking that the result stays under `baseDir`
- `references/golang-general-backend-security.md:475` — - Search for `ServeFile(`, `FileServer(`, `http.Dir(`, `os.Open(`, `ReadFile(`, `filepath.Join(`.
- `references/golang-general-backend-security.md:536` — - If subprocess is necessary:
- `references/golang-general-backend-security.md:552` — - Use library APIs instead of subprocesses.
- `references/golang-general-backend-security.md:774` — - `os.Open(filepath.Join(base, user))` without containment checks
- `references/golang-general-backend-security.md:795` — - sink type (template/SQL/subprocess/files/http)
- `references/javascript-express-web-server-security.md:628` — * Look for `fs.readFile`/`createReadStream` on paths derived from requests.
- `references/javascript-express-web-server-security.md:778` — * If subprocess is necessary:
- `references/javascript-express-web-server-security.md:780` — * MUST avoid `exec()` / `execSync()` with attacker-influenced strings
- `references/javascript-express-web-server-security.md:782` — * SHOULD use `spawn()` with an argument array and strict allowlists. Ensure the executable is hardcoded or allow-listed, do not use a user supplied command name.
- `references/javascript-express-web-server-security.md:787` — * `exec(req.query.cmd)`
- `references/javascript-express-web-server-security.md:788` — * `exec(`convert ${userPath} ...`)`
- `references/javascript-express-web-server-security.md:789` — * `spawn('sh', ['-c', userString])`
- `references/javascript-express-web-server-security.md:790` — * `spawn(userString, ['foo'])`
- `references/javascript-express-web-server-security.md:794` — * Search for `child_process`, `exec(`, `execSync(`, `spawn(`, `fork(`.

## Hook evidence

`hooks.repo.json` / `hooks.plugin.json` are merge snippets. They are optional and must not overwrite an existing hook configuration blindly. Hook events remain `ambient` until correlated to a target run.

## Eval traces

For a controlled eval that intentionally invokes this target skill, capture `codex exec --json` and import it with `--attribution confirmed`. Use `candidate` for traces where target invocation is uncertain.
