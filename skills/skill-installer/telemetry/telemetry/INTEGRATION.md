# Integration guide: skill-installer

## Principle

Instrument high-value semantic boundaries only. Do not turn the target `SKILL.md` into an event-by-event logging checklist.

## Minimum semantic surface

1. Establish a run when the target skill is actually selected or when a target-owned entry script begins.
2. Emit `decision` only for branches that materially change workflow, safety, or verification.
3. Emit `verification`, `failure`, and `retry` around evidence-bearing checks.
4. Emit `user.correction` only when a correction is explicitly observed; never infer one.
5. Close the run with `run.finished` and an outcome.

The recorder prints the `run_id` on `start`. Pass that ID explicitly to later semantic events. Full commands may be supplied to `--command`; the recorder hashes them instead of storing them under the default policy.

## Static candidates from the target

These are inspection hints, not runtime facts.

### Decision candidates

- `LICENSE.txt:21` — otherwise, or (ii) ownership of fifty percent (50%) or more of the
- `LICENSE.txt:60` — excluding communication that is conspicuously marked or otherwise
- `LICENSE.txt:78` — use, offer to sell, sell, import, and otherwise transfer the Work,
- `LICENSE.txt:82` — with the Work to which such Contribution(s) was submitted. If You
- `LICENSE.txt:107` — (d) If the Work includes a "NOTICE" text file as part of its
- `LICENSE.txt:114` — documentation, if provided along with the Derivative Works; or,
- `LICENSE.txt:115` — within a display generated by the Derivative Works, if and
- `LICENSE.txt:128` — reproduction, and distribution of the Work otherwise complies with
- `LICENSE.txt:131` — 5. Submission of Contributions. Unless You explicitly state otherwise,
- `LICENSE.txt:144` — 7. Disclaimer of Warranty. Unless required by applicable law or
- `LICENSE.txt:155` — whether in tort (including negligence), contract, or otherwise,
- `LICENSE.txt:156` — unless required by applicable law (such as deliberate and grossly
- `LICENSE.txt:163` — other commercial damages or losses), even if such Contributor
- `LICENSE.txt:172` — of any other Contributor, and only if You agree to indemnify,
- `LICENSE.txt:198` — Unless required by applicable law or agreed to in writing, software

### Verification candidates

- `VERIFICATION.md:3` — - Offline unit tests resolve the default destination to `$HOME/.agents/skills` with an isolated HOME and verify that explicit `--dest` still receives copied skills.
- `VERIFICATION.md:5` — - Python AST and repository validation cover the helper scripts and canonical catalog.
- `tests/evaluation-cases.md:3` — 1. Run `scripts/list-skills.py --format json`; assert valid JSON and an explicit installed annotation for local matches.
- `tests/evaluation-cases.md:4` — 2. Run `scripts/install-skill-from-github.py --help`; assert the source, destination, ref, and download/git method options are exposed.

### Execution candidates

- `scripts/install-skill-from-github.py:10` — import subprocess
- `scripts/install-skill-from-github.py:87` — with open(zip_path, "wb") as file_handle:
- `scripts/install-skill-from-github.py:100` — result = subprocess.run(args, stdout=subprocess.PIPE, stderr=subprocess.PIPE, text=True)
- `scripts/install-skill-from-github.py:298` — shutil.rmtree(tmp_dir, ignore_errors=True)

## Hook evidence

`hooks.repo.json` / `hooks.plugin.json` are merge snippets. They are optional and must not overwrite an existing hook configuration blindly. Hook events remain `ambient` until correlated to a target run.

## Eval traces

For a controlled eval that intentionally invokes this target skill, capture `codex exec --json` and import it with `--attribution confirmed`. Use `candidate` for traces where target invocation is uncertain.
